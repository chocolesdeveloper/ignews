interface Endpoints {
    Dashboard: {
        index: string;
        cliLogin: string;
        cliSignup: string;
    };
}
export declare function buildEndpoints(base: string): Endpoints;
export declare type Base = string;
export declare type ApiEndpoint = string;
export declare function extractDomainFromBase(base: Base): string;
export declare function buildRepositoryEndpoint(base: Base, domain: string): string;
export {};
