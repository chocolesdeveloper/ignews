import { FileContent } from "./paths";
import { Manifest } from "../models/Manifest";
export declare function createManifest(cwd: string, manifest: Manifest): void;
export declare function retrieveManifest(cwd: string): FileContent<Manifest>;
export declare function maybeRepoNameFromSMFile(cwd: string, base: string): string | null;
export declare function patchManifest(cwd: string, data: Partial<Manifest>): boolean;
export declare function updateManifestSMVersion(cwd: string, version: string): boolean;
