import { SliceMock } from "../models/Slice";
export declare function createPathToMock({ path: filePath, from, sliceName, }: {
    path: string;
    from: string;
    sliceName: string;
}): string;
export declare function resolvePathsToMock({ paths, from, sliceName, }: {
    paths: ReadonlyArray<string>;
    from: string;
    sliceName: string;
}): {
    path: string;
    value: SliceMock | undefined;
} | undefined;
