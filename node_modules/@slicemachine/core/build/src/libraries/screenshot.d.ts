import { Screenshot } from "../models";
export declare enum Extensions {
    jpeg = "jpeg",
    jpg = "jpg",
    png = "png"
}
export declare function createPathToScreenshot({ path: filePath, from, sliceName, variationId, extension, }: {
    path: string;
    from: string;
    sliceName: string;
    variationId: string;
    extension: Extensions;
}): string;
export declare function generatePathsToScreenshot({ base, from, sliceName, variationId, }: {
    base: string;
    from: string;
    sliceName: string;
    variationId: string;
}): Array<string>;
export declare function resolvePathsToScreenshot({ paths, from, sliceName, variationId, }: {
    paths: ReadonlyArray<string>;
    from: string;
    sliceName: string;
    variationId: string;
}): Screenshot | undefined;
